import { InputNode, AgentNode, ToolNode, OutputNode, DecisionNode } from './CustomNodes';

export const nodeTypes = {
  input: InputNode,
  agent: AgentNode,
  tool: ToolNode,
  output: OutputNode,
  decision: DecisionNode,
};
